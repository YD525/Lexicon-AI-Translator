# Project Hub evidence

- `01-empty-project-hub.png` shows the empty Project Hub at the default 1280 by 800 window size.
- `02-project-hub-minimum-size.png` shows the same workflow at the required 1100 by 700 minimum.

Both captures use version 2.0.0.13 and demonstrate that project opening, legacy fallback, recent-project state,
active-project state, drag-and-drop guidance, global status, and keyboard help remain visible without clipping.
