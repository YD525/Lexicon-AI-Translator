# Accessibility evidence

- `01-keyboard-shell.png` shows the 1280 by 800 preview shell with the persistent F1 command-discovery action.
- `02-keyboard-command-reference.png` shows the complete keyboard command reference without clipped content.
- `03-translation-minimum-size.png` shows the translation workflow at the required 1100 by 700 minimum with every
  primary action and the command-discovery footer reachable.

Both screenshots were captured from version 2.0.0.12. The screenshots complement, but do not replace, keyboard-only
traversal and UI Automation inspection for each release candidate.
