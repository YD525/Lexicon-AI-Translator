<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="PreviewDesignTokens.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="PreviewFocusVisual" TargetType="Control">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border Margin="-3" BorderBrush="{StaticResource PreviewBrushFocusBlue}"
                            BorderThickness="2" CornerRadius="{StaticResource PreviewRadiusMedium}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PreviewButtonBase" TargetType="Button">
        <Setter Property="MinHeight" Value="{StaticResource PreviewControlHeightCompact}" />
        <Setter Property="Padding" Value="{StaticResource PreviewControlPaddingCompact}" />
        <Setter Property="FontFamily" Value="{StaticResource PreviewFontFamilyUi}" />
        <Setter Property="FontSize" Value="{StaticResource PreviewFontSizeBody}" />
        <Setter Property="Foreground" Value="{StaticResource PreviewBrushTextPrimary}" />
        <Setter Property="Background" Value="{StaticResource PreviewBrushSurfaceInteractive}" />
        <Setter Property="BorderBrush" Value="{StaticResource PreviewBrushBorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource PreviewFocusVisual}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome" Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource PreviewRadiusMedium}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource PreviewBrushAccentGold}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource PreviewBrushSurfaceCanvas}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PreviewButtonPrimary" TargetType="Button" BasedOn="{StaticResource PreviewButtonBase}">
        <Setter Property="Foreground" Value="{StaticResource PreviewBrushSurfaceCanvas}" />
        <Setter Property="Background" Value="{StaticResource PreviewBrushAccentGold}" />
        <Setter Property="BorderBrush" Value="{StaticResource PreviewBrushAccentGold}" />
    </Style>

    <Style x:Key="PreviewButtonSecondary" TargetType="Button" BasedOn="{StaticResource PreviewButtonBase}" />

    <Style x:Key="PreviewButtonDanger" TargetType="Button" BasedOn="{StaticResource PreviewButtonBase}">
        <Setter Property="Foreground" Value="{StaticResource PreviewBrushError}" />
        <Setter Property="BorderBrush" Value="{StaticResource PreviewBrushError}" />
    </Style>

    <Style x:Key="PreviewTextBox" TargetType="TextBox">
        <Setter Property="MinHeight" Value="{StaticResource PreviewControlHeightCompact}" />
        <Setter Property="Padding" Value="{StaticResource PreviewControlPaddingCompact}" />
        <Setter Property="FontFamily" Value="{StaticResource PreviewFontFamilyUi}" />
        <Setter Property="FontSize" Value="{StaticResource PreviewFontSizeBody}" />
        <Setter Property="Foreground" Value="{StaticResource PreviewBrushTextPrimary}" />
        <Setter Property="CaretBrush" Value="{StaticResource PreviewBrushTextPrimary}" />
        <Setter Property="Background" Value="{StaticResource PreviewBrushSurfaceCanvas}" />
        <Setter Property="BorderBrush" Value="{StaticResource PreviewBrushBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource PreviewFocusVisual}" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource PreviewBrushFocusBlue}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.45" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
